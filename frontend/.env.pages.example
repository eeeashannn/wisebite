# Copy this to a temporary shell env before building for Cloudflare Pages.
# This URL must be HTTPS and should point to your cloudflared tunnel.
REACT_APP_API_URL=https://your-backend-subdomain.trycloudflare.com

# Used by `npm run deploy:pages`.
CF_PAGES_PROJECT=your-pages-project-name
